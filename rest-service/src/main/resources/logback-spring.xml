<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender (Your original, correct configuration) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Loki appender (CORRECTED) -->
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://localhost:3100/loki/api/v1/push</url>
        </http>

        <format>
            <!-- Labels definition -->
            <label>
                <!-- Key-value pairs are easier to read and maintain -->
                <pattern>app=rest-service,host=local</pattern>
            </label>

            <!-- Message definition must use its own pattern tag -->
            <message>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </message>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>